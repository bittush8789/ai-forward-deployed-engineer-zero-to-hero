# Practice Tasks: Module 9 - Model Optimization Pipelines

This document outlines step-by-step tasks to compile models into high-performance engines.

---

## Task 1: Check TensorRT CLI Configuration
*   **Goal**: Check compilation help flags for compiling ONNX models to TensorRT.
*   **Step-by-Step Instructions**:
    1. Run compiler check:
       ```bash
       # trtexec --help
       echo "TensorRT compilation command checks configured"
       ```
*   **Verification**:
    Verify compiler parameters.
